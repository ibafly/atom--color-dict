{
    "outputType": {
        "type": "string",
        "default": "rgb",
        "enum": [
            "rgb",
            "hex"
        ]
    },
    "format": {
        "type": "string",
        "default": "kebab-case",
        "enum": [
            "kebab-case",
            "snake_case",
            "camelCase",
            "Start Case",
            "unformat"
        ]
    },
    "colorIndicator": {
        "title": "Indicator before color names, eg: [color/colour/any other Indicator] home world",
        "type": "string",
        "default": "color"
    }
}
