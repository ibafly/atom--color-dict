{
  "varsValueFormat": {
    "title": "Format of the color variable's value",
    "order": 3,
    "type": "string",
    "default": "rgb",
    "enum": [
      "rgb",
      "hex"
    ]
  },
  "symbolCase": {
    "description": "A symbol contains indicator and color name, eg: color_home_world, colourFreshEgg, iro-dark-sakura",
    "order": 2,
    "type": "string",
    "default": "kebab-case",
    "enum": [
      "kebab-case",
      "snake_case",
      "camelCase",
      "Start Case",
      "unformat"
    ]
  },
  "stylusPrefix": {
    "order": 4,
    "title": "Include `$` in variable names in Stylus",
    "type": "boolean",
    "default": "false"
  },
  "colorIndicator": {
    "order": 1,
    "title": "Indicator before color names",
    "description": "eg: [color/colour/any other Indicator]-home-world",
    "type": "string",
    "default": "color"
  },
  "clearSelection": {
    "order": 5,
    "title": "Clear selections after converting",
    "type": "boolean",
    "default": "false"
  }
}
